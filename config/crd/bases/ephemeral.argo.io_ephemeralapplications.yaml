apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ephemeralapplications.ephemeral.argo.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
spec:
  group: ephemeral.argo.io
  names:
    kind: EphemeralApplication
    listKind: EphemeralApplicationList
    plural: ephemeralapplications
    shortNames:
    - ephapp
    singular: ephemeralapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .spec.expirationDate
      name: Expiration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EphemeralApplication is the Schema for the ephemeralapplications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.'
            type: string
          metadata:
            type: object
          spec:
            description: EphemeralApplicationSpec defines the desired state of EphemeralApplication
            properties:
              expirationDate:
                description: 'ExpirationDate is the date when this ephemeral environment
                  should be deleted Format: RFC3339 (e.g., "2024-12-31T23:59:59Z")'
                format: date-time
                type: string
              namespaceName:
                description: 'NamespaceName is the name for the ephemeral namespace
                  If not provided, a random name will be generated: ephemeral-{random}'
                type: string
              path:
                description: Path is the path within the Git repository
                type: string
              repoURL:
                description: RepoURL is the Git repository URL containing the application
                  manifests
                type: string
              syncPolicy:
                description: SyncPolicy defines how the application should be synced
                properties:
                  automated:
                    description: Automated defines if the application should auto-sync
                    properties:
                      prune:
                        description: Prune specifies whether to delete resources during
                          auto-sync
                        type: boolean
                      selfHeal:
                        description: SelfHeal specifies whether to revert resources
                          during auto-sync
                        type: boolean
                    type: object
                  prune:
                    description: Prune specifies whether to delete resources that
                      are no longer defined
                    type: boolean
                  selfHeal:
                    description: SelfHeal specifies whether to revert resources back
                      to their desired state
                    type: boolean
                type: object
              targetRevision:
                default: HEAD
                description: TargetRevision is the Git revision to deploy (branch,
                  tag, or commit)
                type: string
            required:
            - expirationDate
            - path
            - repoURL
            type: object
          status:
            description: EphemeralApplicationStatus defines the observed state of
              EphemeralApplication
            properties:
              argoApplicationName:
                description: ArgoApplicationName is the name of the ArgoCD Application
                  created
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the application's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource."
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the last time the application was synced
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              namespace:
                description: Namespace is the actual namespace created for this ephemeral
                  application
                type: string
              phase:
                description: Phase represents the current phase of the ephemeral application
                enum:
                - Pending
                - Creating
                - Active
                - Expiring
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

